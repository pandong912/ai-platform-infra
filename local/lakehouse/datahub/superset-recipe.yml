source:
  type: superset
  config:
    connect_uri: http://localhost:8088
    username: admin
    password: admin
    provider: db
sink:
  type: datahub-rest
  config:
    server: http://localhost:8080
